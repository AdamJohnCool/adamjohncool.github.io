import { Component, input, output } from '@angular/core';

@Component({
	selector: 'app-projects',
	imports: [],
	template: `
		<p>Work in progress for now, will list this site as one or maybe hide projects for now?</p>
	`,
})
export class Projects {}
